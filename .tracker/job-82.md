# Задача трекера #78

Задача #78.

В профиле перенести фразу стрик сгорит через… пол плашку текущий стрик и выровнять по левому краю

## выполнение

**Задача #78 выполнена.**

Подпись «стрик сгорит через…» перенесена из блока ачивок под карточку «Текущий стрик» и выровнена по левому краю.

Изменения:
- `ProfileScreen.tsx` — карточка «Текущий стрик» обёрнута в `profile__stat-slot`, подпись о сгорании рендерится сразу под ней
- `ProfileScreen.css` — слот выравнивает содержимое по левому краю, у `.profile__burn` убрано центрирование на всю ширину

Локальный коммит не создан: в окружении не настроен git user (менять config по правилам нельзя). Изменения в рабочей копии — трекер подхватит их при push.
