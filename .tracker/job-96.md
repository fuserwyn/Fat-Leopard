# Задача трекера #92

Задача #92.

Нужна опция редактирования текста задачи внутри тикета, если она еще не началась

## выполнение

**Задача #92 выполнена.**

**Бэкенд** (`tracker_board.go`):
- Добавлена проверка `trackerCanEditPrompt` — редактирование доступно только для задач в колонках «Ожидает» / «Аппрув», до старта агента
- В ответ карточки добавлено поле `can_edit_prompt`
- Операция `prompt` отклоняет правку уже начатых задач и возвращает обновлённую карточку

**Фронтенд**:
- В `trackerApi.ts` — функция `trackerPrompt` и тип `can_edit_prompt`
- В модалке задачи — кнопка «Изменить текст», textarea с «Сохранить» / «Отмена» (префикс спринта сохраняется)
- Стили и тесты (Go + Vitest) — проходят

Локальный коммит не создан: в окружении не настроен git user (по правилам конфиг не трогаю). Изменения в рабочей копии на ветке `tracker/92-673`, пуш сделает трекер.
