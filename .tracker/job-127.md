# Задача трекера #123

Задача #123.

Во флоу загрузки фото убрать лишний шаг подтверждения фото галочкой, сразу показывать экран редактирования или загрузки другого фото

## выполнение

Убрал промежуточный экран подтверждения фото в флоу загрузки отчёта о тренировке.

**Что изменилось:**
- После выбора фото (галерея, камера, вставка из буфера) сразу открывается `PhotoCropper` — без экрана с превью и кнопкой «Готово».
- На экране обрезки добавлена кнопка «Загрузить другое фото» (через опциональный `onReplace` в `PhotoCropper`).
- Компонент `PhotoConfirm` и его стили удалены как неиспользуемые.

**Файлы:** `NewWorkoutScreen.tsx`, `PhotoCropper.tsx`, `PhotoCropper.css`; удалены `PhotoConfirm.tsx`, `PhotoConfirm.css`.

Локальный коммит не создан — в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/123-792`, готовы к пушу трекером.

## ревью

Ревью: на ветке tracker/123-792 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/123-792. Тест пройден.
