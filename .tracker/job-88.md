# Задача трекера #84

Задача #84.

Нужно чтобы до трех разных реакций отображались на посте участника стаи, не уводить реакции в три точки

## выполнение

Сделано для задачи #84:

**`ActivityCard.tsx`** — если на посте 1–3 типа реакций, все показываются сразу, кнопка «⋯» не появляется даже на узком экране. При 4+ типах порог сворачивания снижен с 4 до 3.

**`ActivityCard.css`** — у строки реакций включён `flex-wrap: wrap`, чтобы чипы переносились на следующую строку, а не прятались в меню.

**`ActivityCard.test.tsx`** — добавлены тесты для `trainingReactionVisibleCount` (18/18 проходят).

Локальный коммит не создан: в окружении не настроен git user identity (менять config по правилам нельзя). Изменения лежат в рабочей директории на ветке `tracker/84-658`.

## ревью

Ревью: на ветке tracker/84-658 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/84-658. Тест пройден.
