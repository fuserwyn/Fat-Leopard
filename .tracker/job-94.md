# Задача трекера #90

Задача #90.

Когда задача еще не аппрувнута она стоит в статусе аппрув на доске

## выполнение

Сделано для задачи #90.

**Бэкенд**
- Добавлены `trackerAwaitingApproval` и `trackerEffectiveDevColumn`: задачи с `needs_approval` и неполным числом аппрувов отдаются на доску в колонке «Аппрув» со статусом «Аппрув», даже если в базе ещё `todo`.
- `trackerTaskDueForStart` и `ClaimDueTrackerTasks` не забирают такие задачи в работу по расписанию.

**Фронтенд**
- Функция `boardDevColumn` группирует неаппрувнутые карточки в колонке «Аппрув».

**Тесты**
- Go- и vitest-тесты проходят.

Локальный коммит не создан: в окружении не настроен git user (менять config по правилам нельзя). Изменения в рабочей копии на ветке `tracker/90-667` — трекер подхватит их при push.

## ревью

Ревью: на ветке tracker/90-667 config.go целый. Можно на тест.
