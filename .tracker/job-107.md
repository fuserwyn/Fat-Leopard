# Задача трекера #103

Задача #103.

Удали

## выполнение

Задача #103: из Telegram-уведомления о выкате убрана строка «Сделано для задачи #N.» (на скриншоте в задаче она была обведена).

**Изменения в `tracker_notify.go`:**
- Добавлен фильтр шаблонной фразы агента «Сделано для задачи #N.»
- Функция `trackerEffectiveDoneSummary` отбрасывает эту фразу
- В DM о выкате остаются только заголовок и строка про прод; осмысленное описание работы по-прежнему показывается, если оно не шаблонное
- При записи шага «сделано:» шаблон тоже не сохраняется

**Тест:** `TestTrackerFullyDoneNoteSkipsDoneForTaskBoilerplate` в `tracker_run_test.go`.

Локальный коммит не создан — в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/103-698`, трекер может закоммитить и запушить сам.

## ревью

Ревью: на ветке tracker/103-698 config.go целый. Можно на тест.
