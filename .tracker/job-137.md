# Задача трекера #133

Задача #133.

Добавь новый вид спорта: зарядка

## выполнение

Добавлен вид спорта **Зарядка** (`morning_exercise`, эмодзи ☀️).

**Мини-апп:** тип и пункт в форме тренировки и фильтре ленты (`workoutCategories.ts`), маппинг в текст отчёта (`App.tsx`), эмодзи реакций (`packFeed.ts`), тесты.

**Бэкенд:** распознавание «зарядка», коэффициент кубков 0.8 (как у йоги/растяжки), подпись для AI, порядок типов и реакции в ленте (`training_cups.go`, `leopard_money_training_done.go`, `workout_suggest.go`, `emojis.go`), тесты Go.

Go-тесты и vitest для `workoutCategories` проходят. Локальный коммит не создан: в окружении не настроен git user (конфиг не менял).

## ревью

Ревью: на ветке tracker/133-838 config.go целый. Можно на тест.
