# Задача трекера #116

Задача #116.

Перетащить строку неделя стаи на место текущей строки тренировки стаи. Убрать строку тренировки стаи и иконку обновления. А на этом месте разместить текущую строку неделя стаи но без подложки, бед фона и обводки

## выполнение

Сделано:

- **«Неделя стаи»** перенесена из шапки ленты на место бывшей строки «Тренировки стаи» — сразу над списком карточек.
- Удалены заголовок **«Тренировки стаи»**, кнопка **обновления** (↻) и связанный state `manualRefreshing`.
- Для нового положения добавлен модификатор `feed__pack-progress--plain`: без фона, padding и обводки (прогресс-бар и кнопка «?» остались).
- В шапке остались только бренд и стрик.
- Удалены неиспользуемые CSS-стили секции и кнопки refresh.
- Тесты `FeedScreen.test.tsx` проходят (7/7).

Изменения в `FeedScreen.tsx` и `FeedScreen.css`, не закоммичены (нет git user identity в окружении). Трекер может закоммитить при пуше.

## ревью

Ревью: на ветке tracker/116-769 config.go целый. Можно на тест.
