# Задача трекера #73

Задача #73.

В профиле пользователя убрать описание «тренировки по дням» и «Стрик по дням»

## выполнение

Из профиля убраны два графика в разделе «Ачивки» — «Тренировки по дням» и «Стрик по дням».

В `ProfileScreen.tsx`:
- удалены компоненты `ProfileDayChart` с этими заголовками;
- убрана загрузка и обработка `workouts_by_day` из API;
- удалён эффект перезагрузки профиля при изменении счётчика тренировок (он был нужен только для графиков).

Локальный коммит не создан — в окружении не настроен git user identity. Изменения готовы в рабочей директории, трекер может закоммитить при пуше в `tracker/73-602`.
