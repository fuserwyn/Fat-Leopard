# Задача трекера #131

Задача #131.

В ленте тренировок вместо «инт.» писать полностью слово «интенсивность»

## выполнение

Сделано для задачи #131.

**Изменения:**
- Новые отчёты из мини-аппа формируются как «… мин, интенсивность N/5» (`App.tsx`).
- В ленте старые записи с «инт.» автоматически раскрываются в «интенсивность» при отображении карточки (`expandTrainingIntensityLabel` в `workoutCategories.ts`).
- Бэкенд принимает оба формата при разборе отчёта и начислении кубков (`training_cups.go`).
- Обновлены моки ленты и тесты.

Тесты miniapp (57) и Go проходят. Локальный коммит не создан — в окружении не настроен git user; изменения в рабочей копии на ветке `tracker/131-830`, готовы к пушу трекером.
