# Задача трекера #125

Задача #125.

Ты все еще не убрал шаг с экраном сразу после загрузки фото из медиатеки. Нужно убрать экран на котором пользователь просто подтверждает фото синей галкой. Сразу показывай экран следующий за ним.

## выполнение

Сделано для задачи #125.

**Что изменилось**
- Убран промежуточный экран `PhotoConfirm` (превью + кнопка «Готово»).
- После выбора фото из галереи, камеры или вставки из буфера сразу открывается `PhotoCropper` — можно обрезать, нажать «Готово» без изменений или загрузить другой снимок.
- Компонент `PhotoConfirm` и его стили удалены.

**Файлы:** `NewWorkoutScreen.tsx`; удалены `PhotoConfirm.tsx`, `PhotoConfirm.css`.

Сборка `npm run build` проходит. Локальный коммит не создан — в окружении не настроен git user; изменения готовы к пушу трекером.

## ревью

Ревью: на ветке tracker/125-798 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/125-798. Тест пройден.
