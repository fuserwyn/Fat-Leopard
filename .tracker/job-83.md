# Задача трекера #79

Задача #79.

Сделать строку стрик сгорит в одну строку без переноса и черным цветом

## выполнение

Сделано для задачи #79:

**Стили подписи «стрик сгорит через…»** в профиле:
- `white-space: nowrap` — текст не переносится на вторую строку
- цвет изменён с оранжевого (`#ff7a45` / `#c2410c`) на чёрный (`#000`) в `ProfileScreen.css` и в переопределении для темы leopard в `index.css`

Локальный коммит не создан: в окружении не настроен git user (менять config по правилам нельзя). Изменения в рабочей копии на ветке `tracker/79-619` — трекер подхватит их при push.

## ревью

Ревью: на ветке tracker/79-619 config.go целый. Можно на тест.
