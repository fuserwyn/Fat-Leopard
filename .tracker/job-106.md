# Задача трекера #102

Задача #102.

Нужна опция ставить аппрув прям на доске в трекере

## выполнение

Сделано для задачи #102.

**Что изменилось**

На карточках в колонке «Аппрув» появились кнопки **«Аппрув (N/2)»** и **«Отклонить»** — аппрув можно ставить прямо с доски, без открытия карточки. Клик по кнопке не открывает модалку (`stopPropagation`), вызывается уже существующий API `trackerApprove`.

**Файлы**
- `miniapp/src/components/TrackerScreen.tsx` — логика `canApproveOnBoard`, `approveFromBoard`, кнопки на `TaskCard`
- `miniapp/src/components/TrackerScreen.css` — стили кнопок на карточке
- `miniapp/src/components/TrackerScreen.test.tsx` — тест на кнопку аппрува на доске

Подсказка при создании задачи обновлена: «Два аппрува в Telegram **или на доске**».

TypeScript-сборка проходит. Локальный коммит не создан — в окружении не настроен git user (конфиг не менял). Изменения в рабочей копии, трекер может закоммитить и запушить сам.

## ревью

Ревью: на ветке tracker/102-695 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/102-695. Тест пройден.
