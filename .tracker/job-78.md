# Задача трекера #74

Задача #74.

Убрать в профиле общий заголовок ачивки

## выполнение

Убрал в профиле общий заголовок «Ачивки · X/Y» над блоком ачивок.

Изменения:
- `ProfileScreen.tsx` — удалён `<h2>` с суммарным счётчиком и неиспользуемые переменные `totalAchEarned` / `totalAchMax`
- `ProfileScreen.css` — удалены стили `.profile__achievements-title`

Подзаголовки «Ачивки за стрики» и «Ачивки за тренировки» с отдельными счётчиками остались без изменений.

Локальный коммит не создан: в окружении не настроен git user (менять config по правилам нельзя). Изменения лежат в рабочей копии — трекер подхватит их при push.

## ревью

Ревью: на ветке tracker/74-605 config.go целый. Можно на тест.
