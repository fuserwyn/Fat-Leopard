# Задача трекера #132

Задача #132.

В профиле в теме стая исправить на открытие темы когда наберем за неделю 50 а не 100 тренировок

## выполнение

Сделано для задачи #132.

**Проблема:** логика разблокировки темы «Стая» уже работала с порогом **50** тренировок (`PackWeeklyWorkoutGoal` на бэкенде, `PACK_WEEKLY_GOAL_DEFAULT` на фронте), но в профиле оставались старые тексты про **100**.

**Изменения:**
- `ProfileScreen.tsx` — подсказка `title` у кнопки темы и текст блокировки теперь используют `PACK_WEEKLY_GOAL_DEFAULT` (50)
- `index.css` — обновлён комментарий к теме «Стая»

Локальный коммит не создан — в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/132-835`, готовы к пушу трекером.

## ревью

Ревью: на ветке tracker/132-835 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/132-835. Тест пройден.
