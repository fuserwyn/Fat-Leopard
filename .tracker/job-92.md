# Задача трекера #88

Задача #88.

Нужна фича, что задачу можно перезапустить в админке на доске

## выполнение

Сделано для задачи #88.

**Бэкенд**
- Добавлена операция `restart` в API доски (`localTrackerRestart`, `trackerCanRestart`).
- Перезапуск сбрасывает ошибку и QA, для завершённых/отменённых — ещё и результат, переводит карточку в «В работе» и снова запускает агента.
- В ответ карточки добавлено поле `can_restart` (false, пока агент реально работает).

**Фронтенд**
- На карточках доски появилась кнопка «↻ Перезапустить» (для задач с `can_restart`).
- В модалке задачи кнопка «Перезапустить» теперь вызывает отдельный `trackerRestart`, а не `reschedule`.
- Добавлены стили и тесты.

Go- и vitest-тесты проходят. Коммит не создавал — изменения в рабочей копии на ветке `tracker/88-661`.

## ревью

Ревью: на ветке tracker/88-661 config.go целый. Можно на тест.
