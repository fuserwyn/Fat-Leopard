# Задача трекера #97

Задача #97.

Сейчас в статусе ожидания, задача созданнная раньше стоит ниже той, которая созданная позже, нужно чтоб самая старая созданная задача стояла выше в статусе ожидает по принципу fifo

## выполнение

Задача #97 выполнена.

**Проблема:** колонка «Ожидает» наследовала порядок из API (`ORDER BY id DESC` — новые сверху), поэтому более старые задачи оказывались ниже.

**Исправление:** в `TrackerScreen.tsx` добавлена сортировка `sortTodoColumnFifo` — для колонки «Ожидает» задачи упорядочиваются по `created_at` по возрастанию (при равных датах — по `id`).

**Тест:** в `TrackerScreen.test.tsx` проверка, что при ответе API `[новая, старая]` на доске `#1` отображается выше `#2`.

Коммит не создан: в окружении не настроен git user (по правилам конфиг не меняю). Изменения в рабочей копии, трекер может закоммитить сам.
