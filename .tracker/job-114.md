# Задача трекера #110

Задача #110.

Реакция от Лео на тренировку участника стаи должна быть всегда разной (разные эмодзи) а на аватарку Лео в этой реакции поставь приложенное фото

## выполнение

Сделано для задачи #110.

**Разные эмодзи.** Вместо фиксированного 👍 Лео ставит реакцию из `trainingFeedAllowedEmojis` — эмодзи выбирается по `userMessageID` поста: для каждой тренировки своё, для одного поста стабильное.

**Аватар Лео.** В `packVoters` для `user_id=0` отдаётся URL `leo-avatar.png` (тот же PNG, что в мини-аппе). На фронте в поповере «кто отреагировал» для «Лео» без фото с бэка подставляется `LEO_AVATAR_URL`.

**Файлы:** `miniapp_training_feed_social.go`, `miniapp_feed.go`, `miniapp_feed_avatar.go`, `ActivityCard.tsx`, тесты в `miniapp_feed_avatar_test.go`.

Локальный коммит не создан — в окружении не настроен git user (конфиг не менял). Изменения в рабочей копии на ветке `tracker/110-727`, трекер может закоммитить и запушить сам.
