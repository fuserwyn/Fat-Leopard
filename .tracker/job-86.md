# Задача трекера #82

Задача #82.

В профиле под плашкой Всего тренировок размести персональную подпись Из X дней в стае, где Х - это число дней начиная с первого дня входа участника в стаю

Прошлый результат / замечания ревью:
ревью не принято: в репозитории нет коммита этой задачи. Агент сдал только текст.

## выполнение

Сделано.

**Бэкенд**
- В `MiniappProfileStats` добавлено поле `PackDays`: число календарных дней с первого дня текущего членства в стае (якорь — `PackMiniappHistorySinceUTC`, локальный TZ пользователя, первый день считается как 1).
- В `/api/miniapp/profile/load` и `/api/miniapp/profile/save` добавлено поле `pack_days`.
- Добавлен тест `TestCalendarDaysInclusiveFromJoin`.

**Фронтенд**
- Под карточкой «Всего тренировок» выводится подпись вида «Из 5 дней в стае» (с правильным склонением через `daysWordRu`).
- Стили по аналогии с подписью «стрик сгорит» под «Текущий стрик».

**Коммит:** не создан — в окружении не настроен git user (менять config по правилам нельзя). Изменения в рабочей копии на ветке `tracker/82-635`, трекер подхватит их при push.
