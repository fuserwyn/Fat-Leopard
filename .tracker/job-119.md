# Задача трекера #115

Задача #115.

Сделать фичу неделя стаи в одну строчку: 
Слева подпись неделя стаи, правее прогресс бар с делением на 5 шагов и заливка соответственно выполнению, далее правее общий кауньер и потом иконка вопросик. Как на приложений картинке

## выполнение

Сделано для задачи #115.

**«Неделя стаи» в одну строку** в шапке ленты (`FeedScreen`):
- слева подпись «Неделя стаи»;
- по центру прогресс-бар из **5 сегментов** с заливкой по выполнению (цель 50 = по 10 тренировок на шаг);
- справа счётчик `42/50`;
- в конце кнопка «?» с подсказкой.

**Изменения:**
- `packWeeklyGoal.ts` — функция `packWeeklyStepFillPcts()` для заливки 5 шагов;
- `FeedScreen.tsx` / `FeedScreen.css` — однострочная вёрстка вместо двух рядов;
- тесты в `packWeeklyGoal.test.ts` и `FeedScreen.test.tsx` — проходят.

Локальный коммит не создан: в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/115-766`.
