# Задача трекера #113

Задача #113.

Сделать цель для недели стаи 50 вместо 100

## выполнение

Сделано: недельная цель стаи изменена с **100** на **50** тренировок.

**Бэкенд** — `ms_leo/internal/bot/pack_weekly_goal.go`: константа `PackWeeklyWorkoutGoal = 50` (прогресс в API, проверка достижения цели, выдача бонусной темы).

**Фронтенд** — `miniapp/src/lib/packWeeklyGoal.ts`: `PACK_WEEKLY_GOAL_DEFAULT = 50` (fallback, пока API не ответил).

**Тесты** — обновлены `packWeeklyGoal.test.ts` и `FeedScreen.test.tsx`; все 8 тестов проходят.

Локальный коммит не создан: в окружении не настроен git user (менять config нельзя). Изменения в рабочей директории — трекер может закоммитить при пуше.

## ревью

Ревью: на ветке tracker/113-750 config.go целый. Можно на тест.
