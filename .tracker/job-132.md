# Задача трекера #128

Задача #128.

Поставь на каждую тренировку где нет реакции реакцию от Лео с учетом правила: только ободряющие эмодзи с эмоцией и только эмодзи совпадающие по смыслу со спортом указанной тренировки. Используй разные эмодзи реакций

## выполнение

Сделано для задачи #128.

**Backfill реакций Лео** — в `training_feed_reaction_backfill.go` добавлена функция `BackfillMissingLeoTrainingFeedReactions`: находит все отчёты `training_done` без реакции от Леo (`user_id = 0`) и ставит эмодзи через `LeoReactionEmojiForReport` — только из одобряющих и подходящих виду спорта, разные для разных постов (стабильно по id).

**Миграция 84** — при деплое автоматически проставит реакции на все старые тренировки без них. Уже существующие реакции Лео не трогаются.

**Тест** — `training_feed_reaction_backfill_test.go` проверяет backfill, идемпотентность и сохранение уже стоящих реакций.

Новые тренировки по-прежнему получают реакцию через `ensureLeoDefaultTrainingFeedReaction` при публикации отчёта.

Локальный коммит не создан — в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/128-815`, готовы к пушу трекером.

## ревью

Ревью: на ветке tracker/128-815 config.go целый. Можно на тест.

## тест

Тест: config.go целый, ветка tracker/128-815. Тест пройден.
