# Задача трекера #87

Задача #87.

Сделать подпись под текущим стриком одной строчкой без переноса с формулировкой: текущий стрик сгорит через. Отмечать персональное время до сгорания стрика и оставить иконку часов. Красным цветом

## выполнение

Сделано для задачи #87.

**Текст подписи** — в `streakBurnLabel` формулировка изменена с «сгорит через …» на «текущий стрик сгорит через …». Время до сгорания по-прежнему считается в локальной зоне пользователя (`streakBurnRemainingMs` / `formatStreakBurnRemaining`).

**Одна строка** — в `ProfileScreen.css` для `.profile__burn` и `.profile__burn-text` добавлены `white-space: nowrap` и `flex-wrap: nowrap`, чтобы иконка часов и текст не переносились.

**UI** — под карточкой «Текущий стрик» остаётся красная строка с SVG-иконкой часов и текстом вида «текущий стрик сгорит через 5 ч 20 мин»; обновление раз в минуту без изменений.

**Тесты** — обновлён ожидаемый текст в `streakLabel.test.ts`, все 25 тестов проходят.

Локальный коммит не создан: в окружении не настроен git user (менять config по правилам нельзя). Изменения в рабочей копии на ветке `tracker/87-655`.
