# Задача трекера #119

Задача #119.

Сделай возможность комментировать мудрость дня)

## выполнение

Сделано для задачи #119 — комментирование «мудрости дня» в ленте стаи.

**Что работает:**
- Бэкенд принимает комментарии к карточкам `daily_wisdom` через существующий API тредов (`packFeedSupportsThread`).
- В `FeedScreen` для мудрости дня включены треды: кнопка «Комментарии», поле ввода, ответы и реакции — как у других постов ленты.

**Изменения:**
- `packFeed.ts` — функция `feedSupportsThread()` (включая `daily_wisdom`), используется в `FeedScreen`.
- `miniapp_training_feed_social.go` — текст уведомления «мудрость дня» при ответе на чужой комментарий.
- Тесты: `packFeed.test.ts`, `LeoWisdomCard.test.tsx`, `miniapp_training_feed_thread_reply_test.go` — проходят.

Локальный коммит не создан: в окружении не настроен git user. Изменения в рабочей копии на ветке `tracker/119-779`, готовы к пушу трекером.

## ревью

Ревью: на ветке tracker/119-779 config.go целый. Можно на тест.
