# Задача трекера #105

Задача #105.

На любую тренировку стаи ставить дефортную реакцию 👍 от Лео

## выполнение

Сделано для задачи #105.

**Изменения:**

1. `miniapp_training_feed_social.go` — добавлена функция `ensureLeoDefaultTrainingFeedReaction`: при появлении отчёта в ленте Лео (user_id=0, username «Лео») ставит реакцию 👍 через `SetTrainingFeedReaction`.

2. `leopard_money_training_done.go` — вызов этой функции при обработке `training_done`, в том же месте, где Лео пишет комментарий в тред.

Реакция ставится на любую тренировку стаи (из мини-аппа и из чата). Уведомление автору не уходит — `notifyFeedPostLiked` игнорирует `likerUserID == 0`.

Локальный коммит не создан: в окружении не настроен git user (конфиг не меняю). Изменения в рабочей копии на ветке `tracker/105-715`.

## ревью

Ревью: на ветке tracker/105-715 config.go целый. Можно на тест.
